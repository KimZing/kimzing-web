// tsconfig.vite.json 是专门用于 vite.config.ts 的 TypeScript 配置文件。
{
  "compilerOptions": {
    // 对于引用项目必须设置该属性
    "composite": true,
    "module": "ESNext",
    "moduleResolution": "Node",
    // 允许使用 import 导入使用 export = 导出的默认内容
    "allowSyntheticDefaultImports": true
  },
  // 生效的文件
  "include": ["vite.config.ts"]
}
